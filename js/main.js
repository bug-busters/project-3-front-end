'use strict';

$(document).ready(function () {

  // hide logout button until user is logged in
  $('#logout').hide();

  // toggle Register/Login navbar link with Logout
  // when user is logged in // $('#logout').show();



});

